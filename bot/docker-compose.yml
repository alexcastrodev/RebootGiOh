services:
  bot:
    build: .
    environment:
      DISCORD_TOKEN: ${DISCORD_TOKEN}
      DISCORD_APP_ID: ${DISCORD_APP_ID}
      DISCORD_GUILD_ID: ${DISCORD_GUILD_ID:-}
      AGENT_URL: ${AGENT_URL:-https://reboot-deck.kurz.fyi}
    restart: unless-stopped
