services:
  app:
    build: .
    ports:
      - "4567:4567"
    environment:
      DB_PATH: /data/deck_agent.sqlite3
    volumes:
      - db_data:/data
    restart: unless-stopped

volumes:
  db_data:
